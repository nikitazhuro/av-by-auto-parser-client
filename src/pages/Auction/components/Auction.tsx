import { Col, Row } from 'antd';

const Auction = () => {
  return (
    <Row style={{
      display: 'flex',
      justifyContent: 'center',
      flexDirection: 'column',
      alignItems: 'center',
      height: '100%',
    }}>
      <h1>
        Auction
      </h1>
    </Row>
  )
}

export default Auction;